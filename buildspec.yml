version: 0.2

phases:
  install:
    commands:
      - echo Entering install phase...
      - echo Nothing to do now
  build:
    commands:
      - echo Build started on `date`
      - gradle bootJar
      - mv build/libs/*.jar socketserver.jar
artifacts:
  files:
    - socketserver.jar